# 2026-07-14 工作日志

## 使用情况图表迁移（仪表盘 → 额度管理）
- 将仪表盘 (`dashboard.py`) 的"使用情况"区域（标题 + 时间范围按钮 + 5个统计卡片 + 缓存命中率环形图）整体迁移到额度管理页面 (`accounts.py`)，置于"消耗明细"表格上方
- `accounts.py` 从 `dashboard.py` 导入 `StatCard` 和 `CacheHitRateChart` 组件类（同级模块导入 `from .dashboard import ...`）
- 相关方法 (`_format_token_count`、`_range_btn_style_*`、`_apply_cache_frame_style`、`_render_legend`、`_update_legend`、`_on_range_changed`、`_refresh_usage`) 在 `AccountsPage` 中重新实现
- `dashboard.py` 中对应方法保留为空实现（pass），避免外部调用报错
- `AccountsPage._refresh_table` 新增调用 `_refresh_usage()` 刷新图表数据
- 修复 `dashboard.py` 中 `AddAccountDialog` 导入路径：`from .pages.accounts` → `from .accounts`
- 修复 `dashboard.py` 缺失 `QProgressBar` 导入

## 消耗明细表格高度优化
- 将 `setMaximumHeight(280)` 改为 `setMinimumHeight(330)`（至少展示10行）
- 整个页面内容用 `QScrollArea` 包裹，超出窗口时可滚动

## 机器码 Base62 编码
- SHA256 哈希后增加 Base62 编码层，输出格式从 64 位 hex 变为 `buddy_` + 43 字符 Base62
- 新增 `_to_base62()` 辅助函数

## 服务端 API 加密传输（AES-256-GCM）
- 按照 API.md 实现 AES-256-GCM 加密传输
- `server_api.py` 重写：新增 `_encrypt_body()` / `_decrypt_body()` 加解密函数
  - 使用 `cryptography.hazmat.primitives.ciphers.aead.AESGCM`（非 pycryptodome）
  - 密钥: `e7283867e8d5a1da2f67de4727f12e26ca4d2f7ae83e51dd208d18e75016ed4a`
  - 格式: `base64(nonce(12) + tag(16) + ciphertext)`
- 新增 `redeem()` / `get_buddykey()` / `report_usage()` 三个加密 POST 接口封装
- `get_credits()` 仍为 GET 明文接口，改为动态获取机器码（不再写死 DEFAULT_USER_KEY）
- `accounts.py` 中卡密兑换改用 `server_api.redeem()` 替代直接 requests.post
- 加密/解密往返测试通过
