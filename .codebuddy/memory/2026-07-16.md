# 2026-07-16

## 修复：模型前缀未在代理请求中去掉
- **问题**：`proxy_server.py` 从 `self.db.get_settings().get("model_prefix")` 读前缀，但 dashboard 只在 `_save_client_config()` 中调用了 `save_setting()`，没有同步到 ProxyDatabase 的 settings 表。
- **修复**：`_save_client_config()` 和 `_toggle_proxy_service()` 中同步写入 `db.update_settings({"model_prefix": ...})`

## 新增：版本管理 — 检测新版本并打开浏览器跳转
- **server_api.py**：新增 `check_version()` 函数，调用 `POST /api/version/check` 加密接口
- **updater.py**：完全重写，只保留服务端接口，删除 GitHub/旧服务器检测代码；去掉每小时定时检查，只在启动+手动触发时检查
- **main_window.py**：用户点"是"时 `webbrowser.open(download_url)` 打开浏览器；删除下载更新相关方法

## 服务器地址改为 HTTPS
- SERVER_BASE: `http://47.83.145.136:8787` → `https://buddy.shengdingit.com/api`
- 所有接口 URL 拼接去掉多余的 `/api/` 前缀

## 打包修复 VERSION 识别
- build.bat + build.yml 添加 `--include-data-file=src/VERSION=src/VERSION`
- updater.py `get_current_version()` 改为多路径查找

## 删除 1.log 生成逻辑
- proxy_server.py 中记录上游响应头到 1.log 的代码已移除

## 新增：CLI 命令行模式
- `src/cli.py`：无窗口命令行入口，支持交互式和单次命令两种模式
  - 交互模式：`python -m src.cli`，输入 help/status/credits/redeem/start/stop/port/buddykey/config/quit
  - 单次命令：`--redeem <卡密>` / `--start` / `--credits` / `--buddykey` / `--port <端口号>`
- `cli_app.py`：独立启动入口（类似 app.py），`python cli_app.py` 即可运行
- 服务启动后自动注入积分到 WorkBuddy

## 新增：WorkBuddy 调试端口 + 积分注入整合
- **inject_credits.py**：改造为可导入模块，新增 `inject()` 和 `inject_credits_to_workbuddy(credits)` 便捷接口
- **setup_debug_port.py**：新增 `setup_and_restart(port)` — 配置快捷方式 → 杀 WorkBuddy → 重启
- **dashboard.py** `_apply_config()`：勾选 WorkBuddy 时调 `setup_and_restart()`
- **proxy_server.py**：服务启动时注入积分余额；对话扣减后实时注入新余额到 WorkBuddy（后台线程）
